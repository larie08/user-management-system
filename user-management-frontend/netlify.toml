[build]
  base = "user-management-frontend"
  publish = "dist"
  command = "npm install && ng build user-management-frontend --configuration production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
